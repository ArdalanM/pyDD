sudo: required
dist: xenial
language: python

python:
  - "3.4"

services:
  - docker

before_install:
  - docker pull beniz/deepdetect_cpu
  - docker run -d -p 8080:8080 -v $TRAVIS_BUILD_DIR:$TRAVIS_BUILD_DIR beniz/deepdetect_cpu

script:
- bundle exec rake test